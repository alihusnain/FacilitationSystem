@{
    ViewBag.Title = "editAttendence";
}

<h2>editAttendence</h2>
<link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.0/jquery.min.js"></script>
<script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
<head>
    <title> </title>
    
</head>
@{
    int size = 0;
    int j = 0;
    string tmp = "";
    List<int> Id = new List<int>();
    foreach (var s in ViewBag.subject)
    {

        size = size + 1;
    }

    
    <form class="form-horizontal" role="form" action="/Admin/updateTable" method="post">
        <div class="container" style="width:800px">
            <table class="table table-striped table-bordered table-condensed">
                <thread>
                    <tr>
                        <th>Subject</th>
                        <th>Level</th>
                        <th>Attendence</th>
                    </tr>
                </thread>
                @for (int i = 0; i < size; i++)
                {

                    <tr>
                        <td>@ViewBag.subject[i]</td>
                        <td>@ViewBag.level[i]</td>
                        @foreach (var ob in ViewBag.edit)
                        {
                            if (ob.SubjectTeacherSectionID.ToString() == ViewBag.secid[j])
                            {
                                Id.Add(ob.TeacherAttendanceID);
                                if (ob.Attendance == "P")
                                {
                                    <td>  <input type="checkbox" name="@ob.TeacherAttendanceID" checked /></td>
                                }
                                else
                                {
                                    tmp = tmp + @ob.TeacherAttendanceID + "";

                                    <td>  <input type="checkbox" name="@ob.TeacherAttendanceID" /></td>
                                }
                                @*<td><input type="text" value="@ob.Attendance" name="@ob.TeacherAttendanceID" />  </td>*@
                                j = j + 1;
                                break;
                            }
                        }

                    </tr>
                }
            </table>

            <input type="submit" style="margin-left:500px"/></div>
</form>
        }
