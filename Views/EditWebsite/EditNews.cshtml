@model EADWebTeacherFacilitationSystem.Models.News

@{
    ViewBag.Title = "Edit News";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<body>
    <div class="container">
        <div class="col-lg-5"></div>
        <h3 class="col-lg-6">Edit News Information</h3>
    </div>

    <div>
        @using (Html.BeginForm("SaveEditNews", "EditWebsite", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
        {
            @Html.HiddenFor(u => u.NewsId)

            <div class="form-group">
                @Html.LabelFor(q => q.NewsTitle, new { @class = "control-label col-lg-3" })
                <div class="col-lg-4">
                    @Html.TextBoxFor(q => q.NewsTitle, new { @class = "form-control" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(q => q.NewsDescription, new { @class = "control-label col-lg-3" })
                <div class="col-lg-4">
                    @Html.TextAreaFor(q => q.NewsDescription, new { @class = "form-control" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(q => q.NewsDate, new { @class = "control-label col-lg-3" })
                <div class="col-lg-4">
                    @Html.TextBoxFor(q => q.NewsDate, new { @class = "form-control" })
                </div>
            </div>

            <div class="form-group">
                <div class="col-lg-offset-3 col-lg-10">
                    <input type="submit" value="Save" />
                </div>
            </div>

        }
    </div>
</body>